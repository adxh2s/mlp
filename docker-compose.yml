version: "3.9"

services:
  app:
    build:
      context: .
      args:
        UID: ${UID:-1001}
        GID: ${GID:-1001}
        USER: ${USER:-adhx2s}
    container_name: mlp_app
    user: "${UID:-1001}:${GID:-1001}"
    environment:
      - PATH=/opt/venv/bin:/home/${USER:-adhx2s}/.local/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
      - MLP_OUTPUTS_DIR=/app/outputs
      - MLP_PROJECT_NAME=nba_project
      - MLP_NOTEBOOKS_DIR=/app/notebooks
      - MLP_NOTEBOOKS_URL=http://localhost:8866
      - MLP_LANG=fr
      - MLP_DOCS_DIR=/app/docs
      - STREAMLIT_SERVER_ENABLE_STATIC_SERVING=true
      - MLP_LOG_FILE=/logs/streamlit_app.log
    volumes:
      - outputs:/app/outputs
      - notebooks:/app/notebooks
      - ./data:/app/data:ro
      - ./docs:/app/docs:ro
      - ./docs:/app/static/docs:ro
      - ./logs:/logs:rw
      - .:/app:rw
    ports:
      - "8501:8501"
    command:
      - /opt/venv/bin/python
      - -m
      - streamlit
      - run
      - streamlit_app.py
      - --server.address=0.0.0.0
      - --server.port=8501
    restart: unless-stopped

  notebooks:
    build:
      context: .
      args:
        UID: ${UID:-1001}
        GID: ${GID:-1001}
        USER: ${USER:-adhx2s}
    container_name: mlp_notebooks
    user: "${UID:-1001}:${GID:-1001}"
    working_dir: /app
    environment:
      - PATH=/opt/venv/bin:/home/${USER:-adhx2s}/.local/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
    volumes:
      - outputs:/app/outputs
      - notebooks:/app/notebooks
      - ./data:/app/data:ro
      - ./docs:/app/docs:ro
      - .:/app:rw
    ports:
      - "8888:8888"
    command:
      - /opt/venv/bin/python
      - -m
      - jupyter
      - lab
      - --ip=0.0.0.0
      - --port=8888
      - --no-browser
      - --allow-root
      - --NotebookApp.token=
    restart: unless-stopped

  voila:
    build:
      context: .
      args:
        UID: ${UID:-1001}
        GID: ${GID:-1001}
        USER: ${USER:-adhx2s}
    container_name: mlp_voila
    user: "${UID:-1001}:${GID:-1001}"
    working_dir: /app
    environment:
      - PATH=/opt/venv/bin:/home/${USER:-adhx2s}/.local/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
    volumes:
      - outputs:/app/outputs
      - notebooks:/app/notebooks
      - ./data:/app/data:ro
      - ./docs:/app/docs:ro
      - .:/app:rw
    ports:
      - "8866:8866"
    command:
      - /opt/venv/bin/python
      - -m
      - voila
      - --Voila.ip=0.0.0.0
      - --port=8866
      - --no-browser
      - --Voila.root_dir=/app
    restart: unless-stopped

volumes:
  outputs:
  notebooks:
